import re

while(True):
    try:
        card = int(input("Number "))
        if card is isinstance(card, int):
            break
    except ValueError:
        continue

str(card)

re.search("", card)
