#include <stdio.h>

int main() {

    char input1[100];
    char input2[100];

    // letter values
    int a, e, i, l, n, o, r, s, t, u = 1;
    int d, g = 2;
    int b, c, m, p = 3;
    int f, h, v, w, y = 4;
    int k = 5;
    int j, x = 8;
    int q, z = 10;

    // input
    printf("Player 1: ");
    scanf("%s", &input1);

    printf("Player 2: ");
    scanf("%s", &input2);


    // winner
    for (int i = 0; i < sizeof(input1); i++) {
        switch (input1[i]) {
            case i == 'a':
                
        }
    }

    return 0;
}
