#include <stdio.h>

int main (int argc, char** argv) {

    char input[99999];

    // grab arg input
    do {
        // grab input text
    } while (argc != 2 || );
    // manipulate input text

    // print result

    return 0;
}
